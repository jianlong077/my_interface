<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.haichao.blog.blog.mapper.BlogDetailsTopMapper" >
    <resultMap id="BaseResultMap" type="com.haichao.blog.blog.entity.vo.BlogDetailsTopVO" >
        <result column="cguid" property="cguid" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="status" property="status" jdbcType="VARCHAR" />
    </resultMap>
    <sql id="Base_Column_List" >cguid,name,status</sql>

    <!-- 根据cuid新增 -->
    <insert id="add" parameterType="com.haichao.blog.blog.entity.vo.BlogDetailsTopVO" >
            insert into blog_details_top
            <trim prefix="(" suffix=")" suffixOverrides="," >
                <if test="cguid != null" >
                    cguid,
                </if>
                <if test="name != null" >
                    name,
                </if>
                <if test="status != null" >
                    status,
                </if>
            </trim>
            <trim prefix="values (" suffix=")" suffixOverrides="," >
                <if test="cguid != null" >
                        #{cguid},
                </if>
                <if test="name != null" >
                        #{name},
                </if>
                <if test="status != null" >
                        #{status},
                </if>
            </trim>
    </insert>
    <!-- 根据cguid更新 -->
    <update id="update" parameterType="com.haichao.blog.blog.entity.vo.BlogDetailsTopVO" >
            update blog_details_top
            <set>
                <if test="cguid != null" >
                    cguid=#{cguid},
                </if>
                <if test="name != null" >
                    name=#{name},
                </if>
                <if test="status != null" >
                    status=#{status},
                </if>
            </set>
            where cguid = #{cguid,jdbcType=VARCHAR};
    </update>
</mapper>